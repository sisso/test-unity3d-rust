enum MessageKind : ushort {
	StartGame = 0,
	CreateObj,
	MoveObj,
	SetInputAxis,
}

table IdPackage {
    id: uint32;
}

table PosPackage {
   id: uint32;
   x: float32;
   y: float32;
}

table StringPackage {
   buffer: string;
}

table BytesPackage {
   buffer: [byte];
}
